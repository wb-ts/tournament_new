/* tslint:disable:no-unused-variable */
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import { RegisterUserManagerComponent } from './register-user-manager.component';

describe('RegisterUserManagerComponent', () => {
  let component: RegisterUserManagerComponent;
  let fixture: ComponentFixture<RegisterUserManagerComponent>;

  beforeEach(async(() => {
    /*TestBed.configureTestingModule({
      declarations: [ RegisterUserManagerComponent ]
    })
    .compileComponents();*/
  }));

  beforeEach(() => {
    //fixture = TestBed.createComponent(RegisterUserManagerComponent);
    //component = fixture.componentInstance;
    //fixture.detectChanges();
  });

  it('should create', () => {
    //expect(component).toBeTruthy();
  });
});
